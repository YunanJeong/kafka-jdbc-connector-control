apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kfk-jdbc-register.fullname" . }}-test-connection"
  labels:
    {{- include "kfk-jdbc-register.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kfk-jdbc-register.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
